/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import CancelamentoModule from './cancelamento.module';

describe('CancelamentoModule', () => {
  let cancelamentoModule;

  beforeEach(() => {
    cancelamentoModule = new CancelamentoModule();
  });

  it('should create an instance', () => {
    expect(cancelamentoModule).toBeTruthy();
  })
});
