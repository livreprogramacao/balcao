/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import AnulacaoModule from './anulacao.module';

describe('AnulacaoModule', () => {
  let anulacaoModule;

  beforeEach(() => {
    anulacaoModule = new AnulacaoModule();
  });

  it('should create an instance', () => {
    expect(anulacaoModule).toBeTruthy();
  })
});
