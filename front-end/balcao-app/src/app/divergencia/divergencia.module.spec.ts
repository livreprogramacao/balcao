/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import DivergenciaModule from './divergencia.module';

describe('DivergenciaModule', () => {
  let divergenciaModule;

  beforeEach(() => {
    divergenciaModule = new DivergenciaModule();
  });

  it('should create an instance', () => {
    expect(divergenciaModule).toBeTruthy();
  })
});
