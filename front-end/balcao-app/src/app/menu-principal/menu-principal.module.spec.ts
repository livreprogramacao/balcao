/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import MenuPrincipalModule from './menu-principal.module';

describe('MenuPrincipalModule', () => {
  let menuPrincipalModule;

  beforeEach(() => {
    menuPrincipalModule = new MenuPrincipalModule();
  });

  it('should create an instance', () => {
    expect(menuPrincipalModule).toBeTruthy();
  })
});
