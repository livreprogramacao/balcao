/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import TrocaModule from './troca.module';

describe('TrocaModule', () => {
  let trocaModule;

  beforeEach(() => {
    trocaModule = new TrocaModule();
  });

  it('should create an instance', () => {
    expect(trocaModule).toBeTruthy();
  })
});
