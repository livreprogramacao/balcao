/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import ReembolsoModule from './reembolso.module';

describe('ReembolsoModule', () => {
  let reembolsoModule;

  beforeEach(() => {
    reembolsoModule = new ReembolsoModule();
  });

  it('should create an instance', () => {
    expect(reembolsoModule).toBeTruthy();
  })
});
